import type { Metadata } from "next";
import { Inter, Poppins } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const poppins = Poppins({
  variable: "--font-poppins",
  weight: ["300", "400", "500", "600", "700"],
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Madhu Fabricators | Industrial Manufacturing & Fabrication Excellence",
  description: "38+ Years of Engineering Quality. Trusted manufacturer of MS/SS furniture, industrial sheds, and electrical panels for government and enterprise clients.",
  keywords: "Madhu Fabricators, Metal Fabrication, Industrial Sheds, Steel Furniture, Electrical Panels, Government Contractor, Gujarat Manufacturing",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${inter.variable} ${poppins.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
